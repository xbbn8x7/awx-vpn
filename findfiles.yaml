---
- hosts: localhost
  tasks:
    - name: find role dirs and files
      find:
        paths: /
        recurse: yes
        file_type: directory
        patterns: '*aloaltonetwor*'
#        excludes: 'python3.6'
      register: outlist
    - debug: msg="{{ outlist.files }}"
#      with_items: "{{ outlist.files }}"