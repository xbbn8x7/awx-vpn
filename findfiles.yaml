---
- hosts: localhost
  tasks:
    - name: find role dirs and files
      find:
        paths: /var/lib/awx
        patterns: "*palo*"
        recurse: yes
      register: outlist
    - debug: msg="{{ outlist.files }}"
#      with_items: "{{ outlist.files }}"